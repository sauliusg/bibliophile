Tests '--url-encode' option, with a file whose content is not URL-encoded, using
a stable result from 'tests/bin/europepmc-json_012' instead of querying the
website, allowing the test to work offline, and most importantly, it doesn't
change the cursor mark every day !
